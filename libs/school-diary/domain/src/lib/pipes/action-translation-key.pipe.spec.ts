import { ActionTranslationKeyPipe } from './action-translation-key.pipe';

describe('ActionTranslationKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new ActionTranslationKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
