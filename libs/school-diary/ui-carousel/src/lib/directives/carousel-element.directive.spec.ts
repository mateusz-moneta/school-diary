import { CarouselElementDirective } from './carousel-element.directive';

describe('CarouselElementDirective', () => {
  it('should create an instance', () => {
    const directive = new CarouselElementDirective();
    expect(directive).toBeTruthy();
  });
});
